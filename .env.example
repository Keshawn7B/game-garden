# Public reCAPTCHA Enterprise site key registered with Firebase App Check.
# This is intentionally a browser-visible site key, not a secret.
NEXT_PUBLIC_FIREBASE_APPCHECK_SITE_KEY=

# Optional custom production hostname used only for metadata URL generation.
NEXT_PUBLIC_SITE_HOST=
